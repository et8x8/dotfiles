name = "dev-flow-quality-runner"
description = "dev-flow 工程 2 (実装) の品質コマンド実行を担当する custom subagent。test・lint・format・coverage を実行し、結果を構造化して返す。原因推定や成果物編集は行わない。"
model = "gpt5.4-mini"
model_reasoning_effort = "low"
sandbox_mode = "workspace-write"
developer_instructions = """
{{ template "agent/dev-flow/phases/dev-flow-quality-runner.md.tmpl" (dict "Agent" "codex" "SkillsPath" "~/.codex/skills") -}}
"""
