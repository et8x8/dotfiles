interface:
  display_name: "Feedback Loop"
  short_description: "過去の応答を分析し、承認後にCodex設定を改善するSkill"
  default_prompt: "Use $feedback-loop to analyze the previous request and response, then propose scoped Codex configuration improvements for approval."

policy:
  allow_implicit_invocation: false
